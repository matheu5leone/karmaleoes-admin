# Build scripts de deps nativas. Não são necessários para o boot em dev:
# sharp: otimização de imagem do Next (opcional em dev; há fallback automático).
# unrs-resolver: resolver nativo do eslint-import (binário pré-compilado já incluso).
allowBuilds:
  sharp: false
  unrs-resolver: false
