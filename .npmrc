# Não reinstalar/verificar deps automaticamente ao rodar scripts (pnpm dev/test).
# Evita o pnpm tentar purgar node_modules no container (sem TTY) quando o
# lockfile muda por deps de teste. Instalações são sempre explícitas (pnpm install).
verify-deps-before-run=false
